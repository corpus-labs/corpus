{
  "name": "corpus",
  "private": true,
  "scripts": {
    "clean": "rm -rf node_modules && lerna clean",
    "start:web": "lerna run dev --scope @corpus-labs/web",
    "build:web": "lerna run build --scope @corpus-labs/web"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
