---
description: A BorrowIcon component.
---

import { BorrowIcon } from './borrow';

A BorrowIcon component which renders a React SVG element.

### Component usage

```js
import { BorrowIcon } from '@corpus/design.icons.borrow';

<SwapIcon width={48} height={48} className="text-teal-500" />;
```
