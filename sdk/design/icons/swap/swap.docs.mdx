---
description: A SwapIcon component.
---

import { SwapIcon } from './swap';

A SwapIcon component which renders a React SVG element.

### Component usage

```js
import { SwapeIcon } from '@corpus/design.icons.swap';

<SwapIcon width={48} height={48} className="text-teal-500" />;
```
